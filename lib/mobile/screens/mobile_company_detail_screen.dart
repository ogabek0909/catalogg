import 'package:flutter/material.dart';

class MobileCompanyDetailScreen extends StatelessWidget {
  const MobileCompanyDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: Container(),
    );
  }
}